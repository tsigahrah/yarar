package yarar.rikai;

/** The logging level types */
enum RikaiLoggingLevel {
    /** Silent mode - no logging, no console output */
    NONE,
    /** Console mode - no logging, only console output. */
    CONSOLE
}